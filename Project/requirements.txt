# Core dependencies
python-dotenv==1.0.0
requests==2.31.0

# LLM Providers (install one or more based on preference)
# OpenAI (older models as fallback)
openai==1.3.0

# Anthropic Claude
anthropic==0.8.1

# Google Gemini
google-generativeai==0.3.2

# Optional: for better text processing
regex==2023.10.3
